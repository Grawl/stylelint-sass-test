{
  "private": true,
  "scripts": {
    "lint-scss": "stylelint test/classic.scss",
    "lint-sass": "stylelint test/regular.sass --custom-syntax postcss-sass"
  },
  "devDependencies": {
    "postcss-sass": "^0.2.0",
    "stylelint": "^8.2.0"
  }
}
